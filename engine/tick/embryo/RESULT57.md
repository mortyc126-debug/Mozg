# Результат шага 57: оценка без данных не обновляется -- след переживает и 1000 кругов тишины

Предрегистрация -- `PRE57_NODATA.md`, закоммичена до кода. `NODATA`: в тишине оценки
мощности входа из мира (`r2`, `r2s`, `zv`, `zv5`) не обновляются. Тождественность:
при `NODATA=0` -- побитово (`id57.js`); при `NODATA=1` вне тишины -- тоже побитово
(`id57b.js`, жизнь и паузы). Движок до шага -- `history/neuron2_run3e.js`.

## Парное сравнение (сиды 3001-3012, по 3 цикла; `wake.js`, `out/wake57.tsv`)

| `F` | условие | `R` смеси | `R` канал S | гибель связей при пробуждении | верные смесей |
|---|---|---|---|---|---|
| 300 | без механизма | 0.770 | 0.956 | 0.522 | 0.261 |
| 300 | **`NODATA`** | **1.000** | 1.176 | 0.177 | 0.046 |
| 1000 | без механизма | 0.826 | 0.899 | 0.479 | 0.226 |
| 1000 | **`NODATA`** | **0.999** | 0.967 | 0.179 | 0.076 |

`R` смесей с механизмом по сидам при `F = 1000`: 0.98-1.00 во всех двенадцати.
**Механизм помогает -- ДА:** 0.999 при пороге 0.9, выше, чем без него, W = 78,
p = 2.4·10⁻⁴ (так же при `F = 300`). Гибель связей при пробуждении -- на уровне
обычной жизни (0.179 при отсчёте 0.166).

## Батарея -- табло не упало, изменение принято (`SCOREBOARD.md`, прогон 4)

Строка **«3 длинная тишина» -- ЕСТЬ:** после 1000 кругов свободной активности смеси
0.999, медленный канал 0.954. Строка 3 засчитана во всех трёх смыслах: путь В, 100
кругов, 1000 кругов. `embryo.env` дополнен `NODATA=1`.

## Что это значит

Одно правило заменило две заплатки шага 56 и сделало лучше их (0.999 против 0.975).
Оно не выключает суд в тишине: связь, слабая до тишины, отмирает и в тишине, связь
уходит вместе с погибшей частью. Застывает только то, что мерит мир, пока мира нет.

## Предсказания §6

1. Тождественность вне тишины -- **да**.
2. Табло не падает, строка 3 прямо остаётся -- **да**.
3. Строка 3 длинная тишина -- ЕСТЬ, смеси около 0.97 -- **да, лучше** (0.999; медленный канал 0.954).
4. Парное сравнение: около 0.97 против 0.75 -- **да, лучше** (0.999 против 0.826).
