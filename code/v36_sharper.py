"""v0.36: тот же вопрос, что в v0.35, но мерой вчетверо точнее и на вдвое
большем числе тканей.

ОТКУДА. v0.35 не уличил правило границы: разница разниц +1.73 п.п. при
разбросе 5.75, d > 0 у 10 сидов из 24, p = 0.85. Но там же было записано
подозрение: разброс 5.75 подозрительно близок к тому, что даёт ОДИН
ТОЛЬКО шум пробы при 80 пробах -- sqrt(0.25/80) = 5.6 п.п.

ПОДОЗРЕНИЕ ПОДТВЕРДИЛОСЬ ИЗМЕРЕНИЕМ. Одна и та же ткань, пять разных
шумов пробы:
    при 80 пробах  -- разброс точности ВНУТРИ ткани 4.18 п.п.;
    при 320 пробах -- 2.04 п.п.
    (разброс МЕЖДУ тканями при этом 5.72 п.п. -- то есть теперь он
    действительно про ткань, а не про измерение).
Иначе говоря, все вердикты версий 0.31-0.35 читались на величине,
сравнимой с шумом собственного измерения.

ЧТО СДЕЛАНО. Проба переписана ПАЧКОЙ: пробы независимы, и считать их по
одной -- чистая потеря. Совпадение пачки с пробой по одной проверено
ПОБИТОВО (урок №34: вторая реализация той же меры обязана совпадать, а не
считаться совпадающей). Ускорение позволило поднять число проб вчетверо
и число тканей вдвое, не увеличив время счёта.

ПРАВИЛО ЧТЕНИЯ -- то же, что в v0.35, и код тот же:
  d(сид) = (строение - активность)|правило выкл
         - (строение - активность)|правило вкл
  при p >= 0.05 записывается "правило не показано виновным", а не
  "правило невиновно";
  величина d рядом с долей сидов всегда.

СИДЫ 2201-2248: первая половина та же, что в v0.35, вторая -- свежая.
Это нарочно: если ответ изменится, надо видеть, от точности меры он
изменился или от других тканей.
"""
import sys

sys.path.insert(0, "code")
import v35_rule_effect_direct as v35

if __name__ == "__main__":
    v35.compare(SEEDS=list(range(2201, 2249)))
