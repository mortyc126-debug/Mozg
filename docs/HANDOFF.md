# HANDOFF.md

## Как читать этот документ (обновлено 2026-09-20)

Документ охватывает **v0.1-v0.58** и склеен из двух слоёв, которые не
надо путать.

| раздел | о чём | статус |
|---|---|---|
| "Цель проекта", "Дальняя цель" | рамка, не меняется | действует |
| "Что реализовано: v0.1-v0.10", "Что НЕ реализовано", "Текущее состояние на момент передачи пакета v0.1-v0.10", "Ограничения воспроизводимости" | ПЕРВАЯ эпоха проекта: причинная роль механизмов на ЗАДАННОЙ структуре | исторический срез, сохранён как записан |
| "v0.11 (фазы 1-5)" | польза следа опыта | завершено |
| **"Что реализовано: v0.12-v0.58"** | ВТОРАЯ эпоха: организация ВОЗНИКАЕТ, а не выдаётся | действует |
| **"Текущее состояние (на 2026-09-20 ... v0.58)"** | где проект сейчас | действует |
| "Ограничения воспроизводимости v0.12-v0.58" | что нельзя цитировать и почему | действует |
| "Вторая линия проекта: движок" | второй проект в этом же репозитории | действует |

Старые срезы НЕ переписаны задним числом: они верны для своего режима и
своего вопроса, и часть их выводов позже пересмотрена измерением (самый
крупный случай -- v0.22: польза следа выросла с +2.33 до +43.85 п.п.
при другой силе связи). Где вывод пересмотрен, это сказано в поздних
разделах, а не правкой ранних.

Полный свод -- `docs/RESULTS_LEDGER.md` (все версии, разборы, контроли и
нумерованные методологические ошибки, №1-№49; в головном списке
"КРИТИЧЕСКИ ВАЖНЫЕ ИСПРАВЛЕННЫЕ ОШИБКИ" их 48, №49 записана в разделе
v0.58 и в головной список ещё не перенесена). Где проект на дорожной карте --
`docs/STAGE_MAP.md`. На чём он стоит -- `docs/PRINCIPLES.md`.

## Цель проекта

Построить и проверить простую импульсную (spiking) модель "развивающейся
нервной ткани": сеть узлов с электрической динамикой, структурным ростом
контактов, синаптической пластичностью и гомеостазом порога, и пошагово
установить, какие функциональные свойства (передача воздействия, память
порядка событий, коллективный ритм, региональная организация) она
действительно проявляет — экспериментально, с контролями и самопроверками,
а не по интуиции.

**Это НЕ биологически валидированная модель эмбрионального развития мозга
и не заявка на что-то похожее на сознание или "цифровой мозг".** Формулировки
такого рода намеренно избегались на всём протяжении работы; каждый
результат сформулирован как свойство конкретной модели при конкретных
условиях, а не как общий вывод о мозге или ИИ.

## Дальняя цель проекта (контекст от автора, записан 2026-09-13)

До этого момента в документах была зафиксирована только БЛИЖНЯЯ цель
(построить и проверить импульсную модель) и отрицание дальней. Сама
дальняя цель не была записана нигде. Записывается здесь со слов автора:

**Замысел -- перенос мозга в цифровое пространство.** Не биологическая
копия, а, словами автора, "ремастер" или "кавер": воспроизвести полный
функциональный аналог мозга, но собранный из материала, родного
железу, а не из имитации биохимии. Исходная посылка: биология и код не
связаны напрямую, поэтому копировать биологию буквально бессмысленно --
но это не запрещает воспроизвести ТО ЖЕ УСТРОЙСТВО на другом носителе.

**Стратегия -- начинать с мозга младенца:** переносить и развивать то,
что формируется первым. Отсюда и вся линия v0.1-v0.6 (рост контактов,
созревание узлов, гомеостаз) -- это не произвольный выбор, а прямое
следствие замысла.

### Как это соотносится с дисциплиной проекта -- ОБЕ части нужны

Противоречия между этой целью и отрицанием выше НЕТ, если различать:
- **отрицание относится к ВЫВОДАМ ИЗ РЕЗУЛЬТАТОВ.** Ни один измеренный
  результат не даёт права говорить о "цифровом мозге" или сознании.
  Эта оговорка ОСТАЁТСЯ в силе и снимать её нельзя;
- **цель относится к НАПРАВЛЕНИЮ РАБОТЫ.** Цель -- не утверждение о
  достигнутом. Записать её не значит ослабить требования к выводам.

Правило для следующих сессий: цель может объяснять, ПОЧЕМУ выбран
эксперимент, но НИКОГДА не служит аргументом в пользу интерпретации
результата. Если формулировка результата звучит лучше от упоминания
цели -- это признак, что формулировка плохая.

### Честная оценка расстояния до цели (на момент v0.11)

**Оценка ниже относится к моменту v0.11 и сохранена как записана.
Текущая -- в разделе "Текущее состояние (на 2026-09-20 ... v0.58)".
Главный её вывод -- "связывающее ограничение есть ОТСУТСТВИЕ
СПЕЦИФИКАЦИИ, а не носитель и не число узлов" -- за 47 версий не
опровергнут и остаётся в силе.**

Что установлено: 80 узлов; STDP усиливает выученное направление
(dW>0 на 36/36); отклик по этому пути быстрее на ~1мс; при жёстком
сроке это даёт ~2-3 п.п. в доле своевременных ответов. Порядок
предъявления сеть различить НЕ может.

Связывающее ограничение -- НЕ носитель и НЕ число узлов.
Ограничение -- ОТСУТСТВИЕ СПЕЦИФИКАЦИИ: не определено операционально,
какие функциональные свойства обязаны воспроизвестись, чтобы результат
считался аналогом. Вся линия v0.11 -- урок этого же рода одним уровнем
ниже: "полезность" была недостижима пятью мерами подряд ровно до тех
пор, пока не была построена задача, где у ошибки есть цена. На уровне
всего проекта стоит та же задача, только крупнее: без проверяемого
определения "аналога" цель не операциональна, и любой объём
вычислений её не приблизит.

Это НЕ довод против цели. Это указание на то, какой шаг реально
следующий: не масштабирование ради масштабирования, а формулировка
критериев, по которым можно отличить приближение к цели от
топтания на месте.

Метод работы: пошаговое расширение модели (версии 0.1-0.5), где почти
каждый эксперимент проходил через цикл "результат -> критический разбор ->
обнаружение методологической ошибки -> исправление -> повторная проверка".
Это нормально и ожидаемо -- не сигнал, что модель "сломана".

## Что реализовано: v0.1-v0.10 (кратко по версиям)

*Первая эпоха проекта. Продолжение -- в разделе "Что реализовано:
v0.12-v0.58" ниже.*

- **v0.1** -- базовая модель: `sim_core.py::simulate()`. Развивающаяся сеть
  80 узлов, структурный рост контактов по расстоянию, STDP-подобная
  пластичность, гомеостаз порога. Показано: связи передают воздействие
  причинно (контроль без передачи даёт строгий ноль); порядок обучения
  (A->B vs B->A) детерминированно записывается в направленные веса;
  функциональный след иногда проявляется как миллисекундный сдвиг спайка
  (устойчиво по 5 состояниям x 3 сети, устойчиво к измельчению `dt`).
- **v0.2** -- картография режимов активности при разных `drive`/`gain`.
  Найден кандидат-режим (`drive=1.25, gain=6.0`) с воспроизводимо высоким
  причинным откликом, но с оговорками о внутрисетевой нестабильности.
- **v0.3** -- происхождение популяционного ритма. Причинно показано: связи
  необходимы для популяционной синхронизации (без них -- Q~0.9-1.05,
  неотличимо от контроля перестановками); адаптация задаёт временной
  масштаб ритма (период ~81/145/270мс при tau=0.1/0.2/0.4с) и не даёт сети
  уйти в непрерывное высокочастотное возбуждение.
- **v0.4** -- региональная организация через перевешивание существующих
  контактов (не рост новых). Результат: неоднородный, не симметричный
  эффект; усиление внутриобластных весов увеличило средний внутриобластной
  охват (+2.3 п.п. за 200мс, неравномерно по областям); симметричного
  межобластного эффекта не найдено. Важная находка метода: ранняя метрика
  `R` (подписанная разница счёта) в окне 0-50мс была почти везде равна
  нулю -- это оказалось ограничением метрики и окна, а не отсутствием
  эффекта (растровая метрика `F` показала изменения почти везде).
- **v0.5** -- сравнение правил РОСТА контактов (не готовых сетей).
  Факторный эксперимент 2x2: бюджет входящих контактов (`K=12`) x
  штраф длины связи (`exp(-d/0.10)`). Структурная часть завершена:
  штраф длины -- доминирующий фактор снижения числа контактов;
  взаимодействие механизмов **выражено** (I=+120 контактов для seed=11:
  без штрафа длины бюджет убирает 124 контакта, со штрафом -- только 4).
  **Функциональная часть завершена** (324 пары: 3 геометрии x 4 условия
  x 3 состояния свободного прогона x 3 тестовых шума x 3 набора
  разреженной стимуляции; см. `v05_functional_full_324.pkl` и
  `v05_functional.py`). Наблюдение (на снимках t=12с): штраф длины (сам
  по себе или вместе с бюджетом) снижает средний `F_200` (охват изменения
  растра за 200мс) на ~1.8-3.2 п.п. относительно исходного правила,
  согласованно по знаку на всех трёх геометриях. Бюджет сам по себе
  направленного эффекта на `F_200` не показал. `R_200` везде близок к
  нулю без выраженного паттерна -- отклик проявляется как изменение
  временного растра, а не как чистое изменение суммарного счёта
  импульсов (то же явление, что и в v0.4). Стимул был разреженным
  (3 случайных узла, не обязательно пространственно смежных), не
  пространственно локальным. Причинный вклад именно топологии (в отличие
  от одновременного снижения плотности контактов при штрафе длины)
  отдельно не изолирован -- см. v0.6 ниже, куда это исследование
  продолжено через продление времени развития.
- **v0.6** -- продление развития до 12/24/48/96с для тех же 12 траекторий
  версии 0.5 (3 геометрии x 4 условия), проверка: задержка развития или
  устойчивое различие траекторий? Метод: честный полный перезапуск
  развития с t=0 в точно той же реализации кода (RNG-состояния НЕ
  сохранялись в v0.5, точное продолжение с 12с невозможно) -- корректность
  подтверждена ПОБИТОВЫМ (`np.array_equal`, без допуска) совпадением
  снимка t=12с и с повторным вызовом `simulate_v05()`, и с архивным
  `v05_four_conditions.pkl`, на всех 12 комбинациях (см. `v06_extended_
  growth.py`, `v06_full_run.py`, `v06_extended_growth_full.pkl`).
  **Структурный результат:** без бюджета входящих контактов "Исходное" и
  "Только длина" сходятся к одному и тому же (или почти одному) полному
  графу допустимых контактов к t=96с (индекс Жаккара -> 0.999-1.000 на
  всех 3 геометриях) -- штраф длины ТОЛЬКО задерживает заполнение, не
  меняет предельное множество связей. С бюджетом "Только бюджет" и
  "Совместное" достигают одинаковой заполненности (~1.0), но НЕ сходятся
  по составу рёбер (Жаккар 0.76-0.78 на всех 3 геометриях) -- порядок
  появления контактов при ограниченных местах закрепляет структурное
  различие необратимо (контакты не удаляются). **Функциональный
  результат** (тот же протокол v0.5 -- 36 warm-прогонов + 324 пары --
  повторён на снимках t=96с, см. `v06_functional_96s.py`,
  `v06_functional_96s_full_324.pkl`): без бюджета средний контраст
  `F_200` ("Только длина" - "Исходное") уменьшается с ~-0.018..-0.032
  на t=12с до ~-0.002..+0.003 на t=96с, теряя согласованный знак между
  геометриями -- согласуется с "различие было задержкой развития". С
  бюджетом контраст ("Совместное" - "Только бюджет") НЕ сохраняет
  направление, несмотря на устойчивое структурное различие: близок к
  нулю на 2 из 3 геометрий, меняет знак на положительный (+0.020) на
  третьей (seed=11) -- устойчивое различие графа НЕ гарантирует
  устойчивого направления функционального различия по этой метрике.
  Точное побитовое воспроизведение функционального протокола на снимке
  t=12с подтверждено ТОЛЬКО для одной пилотной комбинации (seed=11,
  "Исходное", warm_seed=500, test_seed=900, stim_set=0), не для всех
  324 пар.
- **v0.7** -- структурная пластичность: ограниченный оборот
  (удаление+замена) контактов ПОСЛЕ заполнения бюджета, на 6 заполненных
  бюджетных структурах (3 геометрии x 2 истории роста "Только бюджет"/
  "Совместное", снимки t=96с из v0.6). До этого шага контакты только
  накапливались; теперь впервые реализовано удаление. Три политики:
  "none" (контроль, контакты не меняются -- 48 "контрольных отметок без
  вмешательства" на траекторию, НЕ пропуски по нехватке кандидатов),
  "random" (случайный входящий контакт заменяется), "weakest" (входящий
  контакт с минимальным текущим весом заменяется). Вес переносится
  старый->новый (сохраняет сумму входных весов и входящую степень
  получателя точно, с точностью до ошибки округления ~1e-17 при
  пересуммировании float64 -- не побитово, но алгебраически точно;
  мультимножество весов строки сохраняется побитово точно, см.
  v07_rewiring.py::verify_invariants). Расписание получателей
  ПОСТРОЕНО ОДИН РАЗ из стартового списка допустимых получателей и
  идентично для всех трёх политик (обосновано: множество допустимых
  получателей инвариантно при сохранении in-degree на фиксированной
  геометрии -- см. v07_rewiring.py::build_fixed_schedule). Кандидаты на
  новый источник фиксируются ДО удаления старого контакта -- иначе
  старый источник немедленно попадает в свободные и часть замен была бы
  фиктивной (проверено явно: ни разу old_source не равен new_source
  среди применённых событий). Электрическая динамика -- та же
  frozen_step из v05_functional.py (рост/пластичность/нормировка весов/
  обновление порога выключены; адаптация/рефрактерность/передача
  включены) -- перестройка ЕДИНСТВЕННЫЙ механизм изменения связей в
  этом эксперименте. Протокол: 24с, замена каждые 0.5с (48 возможностей
  на траекторию), 2 независимые реализации шума+расписания на структуру
  x политику = 36 траекторий (см. `v07_rewiring.py`, `v07_full_run.py`,
  `v07_rewiring_full.pkl`, ~72 МБ).
  **Структурный результат:** критерий удаления влияет на ход
  перестройки. "random" сильнее меняет итоговый состав контактов
  (Жаккар к исходному графу 0.906-0.925) и чаще восстанавливает ранее
  удалённые контакты (11 из 48 в одном примере). "weakest" чаще
  повторно заменяет НЕДАВНО СОЗДАННЫЕ ЕЮ ЖЕ контакты (18-22 из 48 --
  почти половина событий) и оставляет итоговый граф ближе к исходному
  (Жаккар 0.939-0.949) -- это ОЖИДАЕМОЕ следствие конструкции
  (перенесённый минимальный вес остаётся минимальным при выключенной
  пластичности), не новое эмерджентное наблюдение. Базовая частота
  (~5 Гц) и доля молчащих узлов (0) практически не меняются ни для
  одной политики -- перестройка не разрушает базовый режим активности
  по этим двум показателям (это НЕ устанавливает сохранение всей
  временной организации режима).
  **Функциональный результат** (тот же протокол v0.5/v0.6 -- 3 набора
  стимуляции x 3 тестовых шума на каждом из 36 конечных снимков t=24с,
  БЕЗ дополнительного прогрева -- 324 пары, см. `v07_functional_full.py`,
  `v07_functional_full_324.pkl`): средние парные контрасты малы
  (random-none: -0.0025; weakest-none: +0.0017; weakest-random: +0.0042
  по F_200) и БЕЗ согласованного знака между 6 структурами (3 геометрии
  x 2 истории роста -- НЕ 6 независимых повторов). Раскладка разброса:
  Существенная часть разброса отдельных пар (точное описательное
  разложение дисперсии по группам "геометрия x история роста", группа
  объединяет оба повтора: within_share=0.90-0.98 в зависимости от пары
  политик, см. RESULTS_LEDGER.md) находится ВНУТРИ групп, а не между
  ними -- то есть в основном отражает разброс по стимулам/шумам/повторам
  перестройки на близких структурах, а не различие самих структур.
  Средние контрасты малы относительно разброса отдельных проверок; эта
  вариабельность НЕ является оценкой неопределённости среднего по
  независимым структурам (оценка SEM с учётом вложенности и связанности
  условий не проводилась). R_200 везде близок к нулю без паттерна,
  как и во всех предыдущих версиях. Ни одна политика не показала
  согласованного изменения охвата относительно контроля -- допустимый
  результат сам по себе, не неудача эксперимента: установлено различие
  МЕХАНИЗМА структурного изменения без установленного различия ЭТОГО
  конкретного функционального следствия за 24с/48 событий. Полезность
  перестройки НЕ оценивалась и не предполагалась.
- **v0.8** -- опыт-зависимая перестройка: впервые совмещены временная
  пластичность весов (точное STDP-правило из v5f.py, БЕЗ роста/
  гомеостаза порога) и замена контактов (v07) в одном прогоне, с
  принудительной стимуляцией повторяющихся последовательностей A->B и
  B->A (группы по 5 узлов, заранее фиксированы, одинаковы для всех
  ветвей/геометрий). Вопрос: влияет ли ПОРЯДОК повторяющихся входных
  событий на то, какие контакты сохраняются, когда пластичность и
  оборот контактов работают совместно? 6 исходных структур (3 геометрии
  x 2 истории роста "Только бюджет"/"Совместное", снимки t=96с из v0.6)
  x 3 механизма (M1: пластичность+замена слабейшего -- основная ветвь;
  M2: пластичность+случайная замена -- контроль отбора; M3: без
  пластичности+замена слабейшего -- контроль зависимости от
  пластичности) x 2 порядка (AB/BA) x 2 заранее выбранных повтора
  шума+перестройки (rewire_seed in {42,43}, noise_seed in {1000,1001})
  = 72 траектории, 24с каждая, 60 последовательностей A->B/B->A
  (период 0.4с, лаг между группами 10мс, старт t=0.2с), 48 отметок
  перестройки (каждые 0.5с). См. `v08_experience_rewiring.py`,
  `v08_structural_metric.py`, `v08_full_run.py`, `v08_unit_tests.py`,
  `v08_experience_full.pkl` (~137 МБ).
  **Критическая техническая находка в ходе разработки (см. RESULTS_
  LEDGER.md пункт 17):** первая версия кода обрабатывала естественный и
  принудительный импульс ОДНОГО узла на ОДНОЙ временной отметке как ДВА
  отдельных пакета STDP/передачи/сброса (двойной учёт), и могла создавать
  искусственный порядок "раньше/позже" между РАЗНЫМИ узлами, сработавшими
  на одной отметке (второй STDP-вызов видел уже обновлённые trace первого).
  ИСПРАВЛЕНО: единая семантика шага -- natural_fired и forced_fired
  объединяются в одну маску ДО единственного вызова STDP/передачи/сброса.
  Проверено 4 искусственно сконструированными unit-тестами (не полагаются
  на случайное совпадение в симуляции) и подтверждено на реальных
  коллизиях (12 совпадений перестройка+стимул за 24с, дополнительно 7
  случаев совпадения принудительного импульса с естественной активностью
  того же узла в одном примере -- оба типа обрабатываются корректно).
  **Контроли причинной изоляции:** M2 (случайная замена+пластичность) и
  M3 (замена слабейшего без пластичности) дали ТОЧНОЕ совпадение
  структурной истории AB/BA (время, получатель, источники, итоговые
  контактные матрицы) на ВСЕХ 24 проверенных комбинациях (12х2 порядка) --
  подтверждает, что структурный выбор в этих ветвях действительно не
  зависит от порядка стимуляции, как и должно быть по конструкции.
  **Основной результат (M1):** направленный контраст сохранения исходных
  контактов `C = 1/2*[(S_AB(A->B)-S_BA(A->B)) + (S_BA(B->A)-S_AB(B->A))]`
  положителен на 6 из 12 комбинаций (геометрия x история роста x повтор)
  и равен нулю на остальных 6 -- НИ ОДНОГО отрицательного значения,
  среднее +0.103. Раздельные составляющие `C_A = S_AB(A->B)-S_BA(A->B)` и
  `C_B = S_BA(B->A)-S_AB(B->A)` ТОЖЕ ни разу не отрицательны на полной
  таблице из 12 комбинаций (C_A: 5 положительных/7 нулевых; C_B: 3
  положительных/9 нулевых) -- подтверждает симметричную формулировку
  "AB не хуже сохраняет A->B, и симметрично для BA/B->A" на полном
  наборе, не только на одном примере. Соотношение положительных случаев
  C_A (5/12) и C_B (3/12) отмечено как наблюдение, без интерпретации
  асимметрии -- при такой выборке объяснять её пока рано. Первое в проекте
  подтверждённое свидетельство того, что СОДЕРЖАНИЕ опыта (не только
  факт стимуляции) может влиять на последующий структурный отбор.
- **v0.9** -- функциональная проба на сохранённых сетях v0.8 (совокупное
  проявление приобретённых весов и контактов, БЕЗ нового обучения).
  Вопрос: меняет ли история обучения AB/BA последующую реакцию на A
  или B, если кратковременное стартовое состояние ОДИНАКОВОЕ, а веса и
  контакты сохраняют результат обучения? Все 72 конечных экземпляра
  v0.8 (3 геометрии x 2 истории роста x 2 повтора обучения x 3
  механизма x 2 порядка) x 2 направления (A->B, B->A) x 3 тестовых
  шума (900/901/902) = 432 пары "стимул/фон". Общий контролируемый
  старт (диагностический сброс, НЕ имитация естественного пробуждения):
  v/adaptation/refractory/threshold/drive из снимка v0.6 (t=96с, ДО
  начала опыта v0.8) -- идентичны AB/BA; `syn` обнулён в обеих ветвях;
  веса и контакты -- КОНЕЧНЫЕ, приобретённые в v0.8 (различны AB/BA).
  Электрическая динамика -- v05_functional.py::probe (та же проверенная
  семантика v0.5-v0.7, без второго пути обработки импульса). Основной
  показатель: `D = mean_{i,k} |R_AB[i,k] - R_BA[i,k]|`, R в окнах по
  5мс, усреднение по 3 тестовым шумам ПЕРЕД вычислением D (не то же
  самое, что среднее отдельных значений D по каждому шуму -- в общем
  случае `||mean(ΔR)||_1 <= mean(||ΔR||_1)`, равенство -- частный
  случай, не гарантия). См. `v09_functional_probe.py`,
  `v09_full_run.py`, `v09_functional_probe_full.pkl` (~14 МБ).
  **Технический контроль (M3, без пластичности):** веса, контакты и
  ВСЕ растры (baseline и stimulated, оба направления) совпали ТОЧНО
  между AB/BA на всех 12 комбинациях (36 отдельных проверок с учётом
  test_seed) -- контроль реализации общего старта пройден полностью,
  без единого исключения.
  **Результаты по механизмам:** M3 -- D=0.0000 на всех 12 комбинациях,
  оба направления (0/12 ненулевых). M2 (пластичность+случайная замена)
  -- контакты AB=BA ТОЧНО на ВСЕХ 12 комбинациях (согласуется с
  контролями v0.8: случайный выбор не зависит от весов/активности),
  веса при этом различаются -- D>0 на 4/12 (A->B) и 5/12 (B->A)
  комбинаций: в этих случаях различие ОБУЧЕННЫХ ВЕСОВ САМО ПО СЕБЕ
  (при идентичном графе) достаточно для различия вызванного ответа.
  M1 (пластичность+замена слабейшего, основная ветвь v0.8) -- контакты
  AB!=BA практически везде (прямая проверка на полном графе, НЕ через
  показатель C из v0.8, который описывает только исходные A-B/B-A
  контакты и не гарантирует совпадения графа целиком) -- ОДНОВРЕМЕННО
  различаются и веса, и контакты; D>0 на 6/12 (оба направления) --
  функциональный вклад весов и вклад структуры в этом механизме
  ЭТИМ ТЕСТОМ НЕ РАЗДЕЛЕНЫ.
- **v0.10** -- контроль M0: та же пластичность весов, что M1/M2, но
  перестройка контактов ВЫКЛЮЧЕНА (rewire_policy="none" в отдельном
  изолированном ядре v10_no_rewiring.py -- скопировано из v08_
  experience_rewiring.py БЕЗ изменения зафиксированного v0.8 кода;
  побитовое совпадение с v0.8 при одинаковой политике подтверждено
  unit-тестом). Вопрос: меняет ли включение перестройки контактов
  результат обучения относительно сети, где меняются только веса? Это
  совокупный эффект включения перестройки (не чистое влияние
  топологии -- перестройка влияет и на дальнейшее обучение весов).
  24 новые траектории M0 (3 геометрии x 2 истории роста x 2 повтора x
  2 порядка, тот же протокол v0.8: 24с, 60 последовательностей, 48
  отметок перестройки БЕЗ ВМЕШАТЕЛЬСТВА -- расписание потребляется,
  граф не меняется) + 144 новые функциональные проверки (протокол
  v0.9: общий старт, syn=0, 200мс, D по 5мс окнам) + контрасты
  ΔD_weakest=D_M1-D_M0 и ΔD_random=D_M2-D_M0 против уже сохранённого
  v09_functional_probe_full.pkl (согласованность групп/тестовых шумов/
  snapshot_time_source/bin_ms между файлами проверена программно перед
  сравнением). См. `v10_no_rewiring.py`, `v10_unit_tests.py`,
  `v10_full_run.py`, `v10_m0_control_full.pkl` (~46 МБ).
  **Все проверки инвариантов пройдены на всех 24 траекториях и 144
  парах:** граф M0 не изменился ни разу (побитово равен исходному),
  contacts_AB==contacts_BA на всех 12 комбинациях, степень/пороги
  сохранены, назначенные воздействия совпадают AB/BA, расписание
  отметок перестройки (временные шаги) совпадает с соответствующей
  M1-веткой v0.8 на всех 12 комбинациях.
  **Результат:** D_M0 (среднее 0.0024), D_M1 (0.0024), D_M2 (0.0016) --
  различия средних между тремя механизмами МЕНЬШЕ разброса внутри
  каждого механизма (не интерпретируются как ранжирование механизмов).
  ΔD_weakest (M1-M0) среднее +0.0001 БЕЗ согласованного знака (5
  положительных / 4 отрицательных / 15 нулевых из 24 наблюдений --
  "15 нулевых" означает совпадение различимости ответов AB/BA С
  ТОЧНОСТЬЮ ВЫБРАННОЙ МЕТРИКИ И РАЗРЕШЕНИЯ, НЕ означает совпадение
  весов или графов). ΔD_random (M2-M0) среднее -0.0008, тоже без
  согласованного знака (3/7/14). Отдельные отклонения до +-0.0067
  реальны, но их знак не согласован между комбинациями -- НЕ
  суммируются в вывод о направленном влиянии перестройки. Сравнение
  механизмов по числу ненулевых случаев (11/24, 12/24, 9/24) НЕ
  показательно: порог "ненулевой" зависит от численной точности и шума
  той же метрики, что и сами D.
  **Допустимый вывод:** добавление структурной перестройки к
  пластичности весов НЕ изменило систематически масштаб различимости
  вызванных ответов после противоположных порядков обучения в этой
  конкретной пробе. Одна пластичность весов даёт сопоставимую
  различимость ответов AB/BA, что и механизмы с перестройкой. Это НЕ
  означает отсутствия структурных различий или их бесполезности -- это
  означает, что в ДАННОЙ функциональной пробе их вклад не отделился от
  вклада весов.

## Что НЕ реализовано / НЕ проверено (по состоянию на v0.10)

**Список ниже -- срез на конец первой эпохи, сохранён как записан.
Часть его пунктов позже закрыта измерением, и читать их надо вместе с
поздними разделами:**
- "роль тела/сенсорных входов -- вне scope проекта" -- ОТМЕНЕНО: внешнее
  воздействие добавлено в v0.30, петля со средой замкнута в v0.46-v0.48;
- "полезность следа не проверена" -- проверена (v0.11 фаза 5: +2.33 п.п.;
  v0.22 при рабочей силе связи: +43.85 п.п.);
- "масштаб модели 80 узлов" -- начиная с v0.37 ткань растит число
  элементов сама, константы больше нет;
- "любая связь с обучением... не заявлялась" -- остаётся в силе.

Действующий перечень незакрытого -- в разделе "Текущее состояние (на
2026-09-20 ... v0.58)".

- v0.8: функциональная полезность структурного следа НЕ проверена --
  влияет ли различие сохранённых контактов на последующую обработку
  знакомой последовательности? ЧАСТИЧНО ОТВЕЧЕНО в v0.9: совокупное
  (веса+контакты) функциональное проявление подтверждено в части
  условий (см. запись v0.9 выше), но вклад ИМЕННО структурного следа
  отдельно от весов НЕ разделён (это и есть согласованный следующий
  шаг после v0.9 -- пластичность AB/BA БЕЗ замены контактов). Требует
  отдельного разделения вклада изменённых весов от вклада замены
  контактов (одного сравнения "после AB сеть отвечает иначе" для этого
  недостаточно). Неоднородность размера эффекта между геометриями (seed=
  11: C=0 везде; seed=33: до C=+0.45) НЕ объяснена. Малое число исходных
  контактов между группами (2-5) делает показатель S дискретным --
  чувствительность показателя при большем числе контактов не проверена.
  "12 комбинаций" = 3 геометрии x 2 связанные истории роста x 2 повтора
  -- НЕ 12 независимых наблюдений; "6 положительных, 0 отрицательных" --
  не формальный вывод о статистической значимости, наблюдение в
  конкретной проверенной выборке. Сеть не "открыла" механизмы сама --
  группы/последовательности/правило пластичности/критерий удаления/
  расписание перестройки заданы вручную, это не эмерджентное поведение.
- v0.9: дополнительный вклад структурной перестройки ОТНОСИТЕЛЬНО одной
  пластичности НЕ установлен -- M1 (пластичность+замена слабейшего)
  меняет одновременно веса И контакты, поэтому их функциональные вклады
  этим тестом не разделены. Согласованный следующий контроль: пластичность
  AB/BA БЕЗ замены контактов, с теми же исходными сетями/воздействиями/
  шумами обучения -- это тоже НЕ "чистый вклад топологии" (перестройка
  влияет и на дальнейшее изменение весов), а отдельное сравнение с
  одной пластичностью. Полезность приобретённого следа (в смысле
  улучшения какой-либо задачи) НЕ проверялась и не предполагалась --
  D измеряет РАЗЛИЧИМОСТЬ ответов после двух историй, не "качество"
  или "правильность" ответа. Общий контролируемый старт с обнулением
  `syn` -- искусственная диагностическая конструкция, не заявлена как
  модель естественного пробуждения сети после обучения. Разрешение 5мс
  для окон R и D -- выбор анализа, не обоснованный биологический
  масштаб (сырые растры сохранены с полным разрешением модели dt=1мс
  для возможного пересчёта на другом окне без новых симуляций).
- v0.10 ОТВЕТИЛ на вопрос v0.9 про дополнительный вклад перестройки --
  в выбранной пробе (200мс, разреженная стимуляция, D по 5мс окнам)
  вклад НЕ ОБНАРУЖЕН систематически. Но это НЕ доказывает "перестройка
  функционально бесполезна" в общем смысле -- только то, что данная
  конкретная функциональная проба (одно окно, один вид стимуляции,
  одна метрика) не отделяет вклад структуры от вклада весов. Полезность
  приобретённого следа (улучшение какой-либо задачи, а не просто
  различимость ответов) по-прежнему НЕ проверена ни в v0.9, ни в v0.10.
  Другие функциональные пробы (другие окна, другая стимуляция, другие
  метрики) могли бы дать другой результат -- не проверено. Долговременная
  стабильность приобретённого следа НЕ проверялась -- функция тестировалась
  сразу после обучения (снимок t=24с), не после дополнительного периода
  свободной активности или дальнейшего опыта. Масштаб модели (80 узлов,
  малое число геометрий) остаётся ограничением для всех версий v0.5+.

- Механистический контроль для v0.5/v0.6: сохранится ли различие
  `F_200` между правилами при сопоставимом числе контактов -- не
  проверено. Частично снят вопросом v0.6 (без бюджета графы сходятся
  сами, без искусственного уравнивания), но при бюджете плотность
  контактов совпадает (в отличие от их состава), а функциональный
  результат всё равно неоднороден -- изоляция вклада именно топологии
  (не плотности) при бюджете остаётся открытой.
- Перестройка контактов (образование, удаление, замена) -- РЕАЛИЗОВАНА
  в v0.7 как изолированный механистический тест (не связана с опытом/
  пластичностью -- работает при выключенной пластичности весов, поэтому
  "weakest" ориентируется на почти неизменный набор весов, что само по
  себе объясняет наблюдаемое поведение, не требуя дальнейшего изучения
  на этом шаге). Связь перестройки с накопленным опытом (совместная
  работа пластичности весов и оборота контактов) -- НЕ реализована,
  это согласованный следующий этап v0.8.
- Причинное разделение вкладов числа замен, переносимых весов,
  состояния сети и чувствительности измерителя F_200 в (не)обнаруженном
  функциональном эффекте v0.7 -- НЕ проведено. Отсутствие согласованного
  эффекта объяснено ТОЛЬКО отсутствием сигнала полезности НЕ было
  автоматическим выводом -- даже случайная перестройка могла бы
  систематически менять охват; это не проверено отдельно.
- Полная функциональная эквивалентность сетей (в отличие от близости
  одного показателя `F_200`) -- не проверялась и не может быть выведена
  из этого результата.
- Роль весов/порогов/состояний отдельно от графа в сближении или
  расхождении функционального результата v0.6 -- не изолирована (рост,
  пластичность и гомеостаз шли одновременно на всём горизонте 12-96с).
- Независимая воспроизводимость версии 0.4 на других `development_seed`
  (весь региональный анализ -- на одной структуре, seed=11).
- Роль тела/сенсорных входов -- не рассматривалась вообще, вне scope проекта.
- Любая связь с обучением "полезным функциям", распознаванием, памятью
  в содержательном смысле -- не проверялась и не заявлялась. "Полезность"
  изменения связей (в контексте будущей перестройки) ещё не определена
  проверяемым образом.
- Причинная роль конкретных путей (в отличие от глобальных
  агрегированных эффектов) в основном не разделена.

## Текущее состояние на момент передачи пакета v0.1-v0.10 (исторический срез)

*Запись конца первой эпохи. Актуальное состояние -- в разделе "Текущее
состояние (на 2026-09-20 ... v0.58)". В частности, перечисленные здесь
"открытые вопросы для следующей сессии" уже разобраны: полезность следа
-- v0.11 и v0.22, другие функциональные пробы -- v0.19-v0.29,
масштабирование -- v0.37 (ткань растит элементы сама).*

**Версии 0.5-0.10 завершены. Проект v0.1-v0.10 представляет собой
законченный, воспроизводимый пакет с проверяемой историей от базовой
динамики импульсов до опыт-зависимого структурного развития.**

Полная цепочка версий:
```
v0.1  Возбудимость, передача, пластическая запись порядка событий
v0.2  Карта режимов, зависимость от состояния
v0.3  Происхождение ритма: передача + адаптация
v0.4  Перераспределение весов между областями сети
v0.5  Правила роста контактов: бюджет входов + штраф длины связи
v0.6  Продление развития: задержка формирования vs закрепление истории
v0.7  Структурная пластичность: оборот контактов после заполнения бюджета
v0.8  Влияние ПОРЯДКА опыта (AB vs BA) на структурный отбор -- первый
      опыт-зависимый структурный механизм в проекте
v0.9  Функциональное проявление приобретённого следа (совокупно, веса+граф)
v0.10 Контроль: доп. вклад перестройки поверх одной пластичности --
      не обнаружен систематически в выбранной функциональной пробе
```

Итоговая запись v0.10 (последняя завершённая версия): добавление
структурной перестройки к пластичности весов не изменило систематически
масштаб различимости вызванных ответов после противоположных порядков
обучения в исследованной пробе (200мс, разреженная стимуляция, метрика
D по 5мс окнам). Одна пластичность весов (M0) даёт сопоставимую
различимость ответов AB/BA, что и механизмы с перестройкой (M1, M2) --
средние D близки (0.0024 / 0.0024 / 0.0016), различия между механизмами
меньше разброса внутри каждого. Контрасты ΔD_weakest и ΔD_random близки
к нулю без согласованного знака на 24 проверенных комбинациях
(геометрия x история роста x повтор x направление). Это НЕ означает
отсутствия структурных различий или их бесполезности -- означает, что
в ДАННОЙ пробе вклад структуры не отделился от вклада весов. Полный
свод результатов, ограничений и технических находок по всем версиям --
см. RESULTS_LEDGER.md.

**Открытые вопросы для решения СЛЕДУЮЩЕЙ сессии (не выбран
приоритетный, решение сознательно отложено):**
- Полезность приобретённого следа -- показана различимость ответов, не
  улучшение обработки какой-либо задачи.
- Масштабирование -- 80 узлов, малое число независимых геометрий (3) на
  всех версиях v0.5+.
- Соответствие биологии -- модель вдохновлена общими принципами
  (рост, пластичность, оборот связей), не воспроизводит конкретные
  стадии эмбрионального развития.
- Другие функциональные пробы -- 200мс и разреженная стимуляция (v0.9,
  v0.10) это один выбор из многих возможных; другое окно, другой вид
  стимула или другая метрика МОГЛИ бы дать другой результат по вкладу
  перестройки -- не проверялось.
- Долговременная стабильность -- приобретённый структурный след
  проверялся сразу после обучения (снимок t=24с), не после
  дополнительного периода свободной активности или дальнейшего опыта.

## Ограничения воспроизводимости v0.1-v0.11 -- важно не путать

*Продолжение для поздних версий -- в разделе "Ограничения
воспроизводимости v0.12-v0.58" ниже.*

- **v0.1-v0.3, часть v0.4**: "3 сети" почти везде означает `development_seed
  in {11, 22, 33}` -- это 3 НЕЗАВИСИМЫЕ структуры (разные positions, разные
  графы контактов). Это встречается в большинстве ранних протоколов.
- **v0.4 региональный анализ (perturbation/localization тесты)**: только
  ОДНА структура (`development_seed=11`). "5 состояний x 5 шумов" -- это
  5 разных случайных состояний ОДНОЙ и той же структуры, а НЕ 5 сетей.
  Обобщение на другие структуры не проверялось.
- **v0.3 recovery-протокол (36 пар, `recovery_v02/`)**: тоже одна
  структура, разные состояния/шумы.
- **v0.5**: 3 сида проверены для структурного сравнения (числа контактов,
  длины и т.д.), но это тоже "3 независимые структуры", а не десятки --
  устойчивость сравнения на большем числе сидов не проверялась.
- **v0.6**: те же 3 геометрии, что v0.5 (11/22/33) -- НЕ новые независимые
  структуры. Функциональный протокол на t=96с использует ТЕ ЖЕ 3 набора
  стимуляции, warm_seeds, test_seeds, что и на t=12с -- сравнение t=12 vs
  t=96 парное по этим ключам, не независимая повторная выборка. Точное
  побитовое воспроизведение функционального протокола на снимке t=12с
  подтверждено ТОЛЬКО для одной пилотной комбинации (seed=11, "Исходное",
  warm_seed=500, test_seed=900, stim_set=0) -- НЕ для всех 324 пар;
  остальные пары полагаются на то, что тот же код (`v05_functional.py`)
  применён к структурно идентичному (проверено отдельно) снимку t=12с.
- **v0.7**: "6 структур" = 3 геометрии x 2 истории роста ("Только
  бюджет"/"Совместное") -- НЕ 6 независимых повторов, и 2 истории роста
  для одной геометрии тоже НЕ независимы (общая исходная геометрия).
  "2 повтора" на структуру x политику -- это 2 ЗАРАНЕЕ ВЫБРАННЫЕ
  реализации шума+расписания (rewire_seed/noise_seed из фиксированного
  списка [42,43]/[1000,1001]), не случайная выборка -- разведочное
  сравнение, явно названное так в спецификации, не окончательная оценка
  воспроизводимости. Функциональный тест v0.7 (324 пары) применён БЕЗ
  дополнительного прогрева -- в отличие от протокола v0.5/v0.6, который
  включал 12с свободного прогона перед пробами.
- **v0.8**: "12 комбинаций" = 3 геометрии x 2 связанные истории роста x
  2 заранее выбранных повтора (те же rewire_seed/noise_seed, что v0.7)
  -- НЕ 12 независимых наблюдений. Группы A/B выбраны ОДИН РАЗ (seed=100,
  найдены на первой попытке поиска для каждой геометрии, случайно
  оказались идентичными индексами узлов на всех трёх геометриях) и
  одинаковы для ВСЕХ 72 траекторий -- устойчивость результата к другому
  выбору групп НЕ проверена. Исходные контакты между A и B малочисленны
  (2-5 в зависимости от геометрии) -- показатель S дискретен, "6
  положительных из 12" не то же самое, что статистическая значимость.
  Файл `v08_experience_full.pkl` в составе переданного пакета был
  ПОВТОРНО СГЕНЕРИРОВАН после случайной потери исходного файла в ходе
  организационной работы (см. RESULTS_LEDGER.md, раздел v0.8): все
  опубликованные показатели (включая полную таблицу C/C_A/C_B и 24/24
  структурных контроля) воспроизведены точно тем же детерминированным
  кодом с теми же параметрами. Хеш ИСХОДНОГО (утраченного) файла не был
  сохранён заранее, поэтому полное побайтовое совпадение с ним НЕ
  проверено -- утверждается лишь точное воспроизведение опубликованных
  числовых результатов, не идентичность файлов на уровне байт.
- **v0.9**: "12 комбинаций" -- те же самые 12 исходных экземпляров v0.8
  (3 геометрии x 2 истории роста x 2 повтора обучения), не новая
  независимая выборка. Общий контролируемый старт с обнулением `syn`
  -- искусственная диагностическая конструкция, специально
  сконструированная для этого сравнения; результаты (в частности,
  нулевые случаи D) НЕ переносятся автоматически на сценарий без
  сброса `syn` или с другим стартовым состоянием. Тестировалось только
  разрешение 5мс для окон -- чувствительность D к выбору окна (грубее
  или тоньше) не проверена. Одна пара групп A/B (та же, что v0.8) --
  не варьировалась.
- **v0.10**: "24 комбинации" (12 x 2 направления) -- те же геометрии/
  истории роста/повторы, что v0.8/v0.9, не новая выборка. Сравнение
  трёх механизмов (M0/M1/M2) по СРЕДНЕМУ D или по числу ненулевых
  случаев НЕ учитывает, что разница между механизмами меньше разброса
  внутри каждого -- любое ранжирование механизмов по этим числам
  ("M1 лучше M0" и т.п.) было бы избыточной интерпретацией шума, не
  подтверждённым выводом. Отсутствие систематического ΔD НЕ означает
  отсутствия эффекта в принципе -- относится к ЭТОЙ конкретной пробе
  (200мс, разреженная стимуляция группами по 5, окна 5мс).
- Нигде в проекте не проверялась стабильность выводов относительно
  выбора *абсолютных* значений `dt=0.001`, `N=80`, порогов роста и т.д.
  как класса моделей -- только относительно вариаций внутри уже выбранных
  значений.

## v0.11 (фаза 1): полезность следа -- НЕ УСТАНОВЛЕНА

Направление выбрано пользователем: полезность приобретённого следа.
Определения, предсказания и правило чтения зафиксированы ДО запуска
в V011_SPEC.md; полный разбор -- RESULTS_LEDGER.md, раздел v0.11.

Впервые за проект ВАРЬИРОВАНЫ пары групп A/B (3 пары вместо одной) --
это снимает ограничение, висевшее с v0.8. Конвейер воспроизвёл архив
v0.8 ПОБИТОВО (96/96) на совпадающей паре групп.

Итог: заранее объявленное правило (согласие >=2 мер из 3) НЕ выполнено.
- U1 (латентность): +1.61мс в пользу совпадающей по порядку ветви,
  знак одинаков на 31/36 комбинациях и на ВСЕХ парах групп и
  геометриях, исправленный парный перестановочный тест даёт p=0.0001.
- U3 (надёжность): знак ПРОТИВОПОЛОЖНЫЙ, величина 0.64 от собственного
  разброса.
- U2 (декодер): неинформативен по конструкции -- реализованная задача
  не адресует ПОРЯДОК. Дефект спецификации меры.
Полезность НЕ установлена. U1 -- самое сильное направленное
функциональное наблюдение линии v0.8-v0.11, но одна мера по правилу
проекта остаётся наблюдением, а не выводом.

## v0.11 (фаза 2): распознавание порядка -- сигнал не обнаружен

Пункт 1 плана фазы 2 ВЫПОЛНЕН. Задача перепроектирована под
распознавание ПОРЯДКА; проба построена на машинерии v0.8, её
эквивалентность проверенной пробе v0.9 показана ПОБИТОВО (6 сверок).
609 реальных коллизий natural+forced обработаны и посчитаны.

Результат: точность распознавания порядка M1 0.5123, M3 0.5132,
перемешанные метки 0.499 -- всё у случайного уровня. U2a=-0.0009,
U2b=-0.0059 без согласованного знака. Проверка БЕЗ ДЕКОДЕРА: различие
между порядками 0.000473 против порога шума 0.008783, 0 из 72
сравнений выше порога => это отсутствие сигнала, а не слабость
декодера.

Совокупно по трём мерам правило (>=2 из 3) НЕ выполнено:
U1 за полезность, U3 против, U2 ноль. **Полезность НЕ установлена.**

**Ключевое ограничение, осознанное по итогам:** чтобы убрать артефакт
стимуляции, из признаков исключены группы A и B -- но выученный след
живёт ИМЕННО там. Вывод означает "порядок не доходит до остальной
сети", а не "след без функционального эффекта". Чистое считывание с
A/B этой пробой невозможно в принципе: группа B активируется на 60мс
в порядке AB и на 50мс в порядке BA, её рефрактерность тривиально
различается независимо от следа.

## v0.11 (фаза 3): причина U1/U3 найдена и проверена вмешательством

Пункт 1 плана фазы 3 ВЫПОЛНЕН. Гипотеза "быстрее, но менее устойчиво"
ПОДТВЕРЖДЕНА как единый компромисс от одного механизма.

Цепочка:
1. СТРУКТУРА: dW (вес выученного направления, перекрёстная форма)
   положителен на 36 из 36 комбинаций, ни одного отрицательного, знак
   одинаков на всех парах групп и геометриях. Веса +0.0548 (36/36),
   топология +0.0178 (25 полож., 11 нулей, 0 отриц.).
2. ПРИЧИННОСТЬ: пересадка этого блока в несовпадающую ветвь закрывает
   78.8% разрыва латентности (веса) и 104.3% (веса+топология).
   Разрыв +1.1983мс воспроизведён на НЕЗАВИСИМЫХ шумах 980-1019.
3. СПЕЦИФИЧНОСТЬ: равноценный блок от 5 случайных узлов в ту же цель
   закрывает -0.0% (ускорил в 1 ячейке из 72), при этом реально менял
   веса в 66 из 72 ячеек.
4. ЕДИНСТВО МЕХАНИЗМА: тот же блок закрывает 67.4% разрыва по
   НАДЁЖНОСТИ, контроль -0.5%.

Попутно частично закрыт вопрос, открытый с v0.9: вклад весов и
топологии по латентности разделён (78.8% / остальное).

ВАЖНО, что это НЕ меняет: ускорение отклика -- не полезность. Правило
(>=2 меры из 3) по-прежнему НЕ выполнено. **Полезность приобретённого
следа остаётся НЕ УСТАНОВЛЕННОЙ.** И величина эффекта по надёжности
ничтожна (0.009615 -> 0.009667, ~0.5% относительно), "67.4%" -- это
доля крошечного разрыва.

Отбракован первый контроль специфичности (блок обратного направления):
его ноль оказался гарантирован по построению, а не эмпирически --
блок несёт сигнал от B к A, а мерится время ПЕРВОГО импульса в B.
Заменён на контроль по пути, который влиять способен.

## v0.11 (фаза 4): след держится на одной пластичности

Пункт 1 плана фазы 4 ВЫПОЛНЕН. M0 (пластичность, перестройка выключена)
даёт dW>0 на 36 из 36 комбинаций и U1=+0.866мс со знаком, одинаковым на
всех парах групп и геометриях. **Перестройка контактов для следа и
ускорения НЕ НЕОБХОДИМА.**

Парная разность M1-M0 по U1 = +0.20мс при 11 отрицательных из 36 и
всего 0.240 от разброса внутри M0 -- ранжировать механизмы по ней
НЕЛЬЗЯ. Зато перестройка последовательно ПЕРЕРАСПРЕДЕЛЯЕТ след:
снижает весовую компоненту (29/36 отрицательных) и добавляет
топологическую (25 положительных, 0 отрицательных), доля топологии
0.245. Это уточняет вывод v0.10: видно, ЧТО происходит -- след
переносится из весов в топологию почти без изменения функционального
эффекта.

Контроль корректности: вклад топологии у M0 и M2 вышел РОВНО нулевым
(0/36 в обоих) -- независимое подтверждение декомпозиции dW, на которой
держится вывод фазы 3.

## v0.11 (фаза 5): задача с ценой ошибки -- преимущество есть, малое

Пункт 1 плана фазы 5 ВЫПОЛНЕН. Построена задача, где у ошибки есть
цена: цель обязана ответить в срок T, оценка `hit - FA`.

Результат: +2.33 п.п. при основном сроке T=30мс (выбран ПРАВИЛОМ по
медиане латентности контроля, не по эффекту), 29 положительных из 36,
НИ ОДНОГО отрицательного; на коротких сроках до +3.15 п.п.; к 100мс
исчезает. Контроль M3 -- строго ноль на всех восьми сроках.
Эффект держится на ПОПАДАНИЯХ (+2.67 п.п.); ложные тревоги у
совпадающей ветви чуть выше и работают ПРОТИВ неё (-0.33 п.п.).

Расхождение с предсказанием (1.53x) ОБЪЯСНЕНО как грубость самого
предсказания: латентность целочисленная, окна жёсткого сдвига при
T=30 совпадают, а по кривой сроков предсказание то занижает, то
завышает -- дополнительного механизма не требуется.

**ВАЖНО про статус вывода.** Правило фазы 1 (согласие >=2 мер из 3)
НЕ выполнено и этим НЕ отменяется. U5 -- новая, отдельно
предрегистрированная проверка, а не пересмотр старого правила.
Величина мала (~3 п.п. максимум) и не превышает собственный разброс
между комбинациями (0.986). Задача меряет СВОЕВРЕМЕННОСТЬ ответа, а
не его содержательную правильность: различить предъявленный ПОРЯДОК
сеть по-прежнему не может (фаза 2).

**Итог линии v0.11.** Установлено причинно и с контролями:
STDP усиливает выученное направление (dW>0 на 36/36) -> отклик по
этому пути ускоряется на ~1мс -> при жёстком сроке это даёт ~2-3 п.п.
преимущества в доле своевременных ответов. Перестройка контактов для
этого не необходима, она лишь перераспределяет след из весов в
топологию.

**Открытые направления (приоритет НЕ назначен, решение за следующей
сессией):**
- Масштабирование: 80 узлов, 3 геометрии на всех версиях v0.5+.
- Долговременная стабильность следа: проверялся сразу после обучения.
- Задача, требующая РАЗЛИЧЕНИЯ порядка, а не только своевременности --
  но сначала нужна конструкция, в которой порядок вообще доходит до
  считывания (фаза 2 показала, что до остальной сети он не доходит).
- НЕ дорабатывать последовательностную пробу под считывание с A/B --
  ограничение конструктивное, а не параметрическое.

## Что реализовано: v0.12-v0.58 (кратко по версиям)

Записано 2026-09-20. Числа взяты из RESULTS_LEDGER.md (разделы v0.12-v0.58);
здесь только итоги, разборы и контроли -- там.

**Перелом, определивший всю вторую половину проекта.** Начиная с v0.13
организация перестала ВЫДАВАТЬСЯ и начала ВОЗНИКАТЬ (PRINCIPLES §6:
"задать минимум условий и смотреть, что возникнет"). Это другая задача,
чем v0.1-v0.11, и мерки у неё другие (PRINCIPLES §2a: цифровая валюта --
"сколько из состояния можно прочесть", а не "похоже ли на культуру").

### v0.12-v0.21: каждый уровень организации возникает и остаётся немым -- и почему

- **v0.12** -- пространственного распространения НЕТ: разность с
  перестановочным контролем 0.060 от собственного разброса, согласована
  по знаку в 51.8% всплесков. Добавлены задержки проведения (побитовое
  тождество с прежней пробой при нулевой задержке) -- волн ВСЁ РАВНО
  нет. Причина измерена: вклад сети в ток узла 0.68% от drive, режим
  drive-доминированный. Синхронизация (v0.3) и распространение --
  РАЗНЫЕ явления; модель даёт первое без второго.
- **v0.13** -- позиции больше не выдаются: ткань растёт из ОДНОГО агента
  делением и расталкиванием, индекс Кларка-Эванса 1.17 против 1.05 на
  10 сидах из 10. Функциональное сравнение отложено СОЗНАТЕЛЬНО: у
  выросших позиций 29.7 допустимых соседей против 12.3 (ловушка №13).
- **v0.14** -- при выравнивании плотности ДВУМЯ независимыми способами
  (сошлись до 0.1%) отличие исчезает: 7 из 12, отношение 1.007. Без
  выравнивания было бы +16.6% и 12 из 12. **Отличие было плотностью, а
  не организацией.**
- **v0.15** -- различия элементов на 86% ВЫДАНЫ: drive 81.9%, расписание
  появления 4.1%, собственный шум 12.9%, взаимодействие 1.1% и лишь на
  8 сидах из 12. Механизма дифференцировки в модели НЕТ -- не слабый, а
  отсутствующий.
- **v0.16** -- введено локальное соперничество состояний. Одинаковые
  элементы РАСХОДЯТСЯ: доля у краёв 0.949, без соседей 0.057,
  |s_i - s_j| выше перестановочного нуля на 24 сидах из 24, при другом
  шуме согласие 0.470 (картина НЕ считывается из геометрии). `s` при
  этом ни на что не влияет -- это выбор устройства проверки, дающий
  побитовое тождество с выключенным механизмом.
- **v0.17** -- состояния складываются в ДОМЕНЫ с характерным масштабом;
  масштаб задаётся дальностью правила (максимум различия на 0.8-0.85
  радиуса при трёх разных радиусах). Дальность от плотности НЕ разделена.
- **v0.18** -- состояние впервые получает следствие (различие мешает
  связи вырасти): узор углубляется на 20 сидах из 24 (p = 0.00033), но
  прямая мера сдвигается лишь на 6%, а согласование активности не
  меняется (16 из 24). Граница как СТРУКТУРА есть, как функция -- нет.
- **v0.19** -- проверена гипотеза "немота есть свойство ЛИНЕЙКИ":
  биологическая мера заменена цифровой (адресуемость -- говорит ли
  активность, ГДЕ произошло событие). **Гипотеза ОПРОВЕРГНУТА:** 0.56
  при пороге 0.65, одинаково в обоих условиях. Попутно опровергнута и
  вторая гипотеза (граф не пространственен): до семи шагов, связь шагов
  с расстоянием r = 0.95.
- **v0.20** -- ОТОЗВАН: множитель силы связи был вписан только в
  развитие, а проба всегда шла при силе 1.0 (ошибка №34). Числа
  недействительны, вопрос закрыт в v0.21.
- **v0.21** -- **АДРЕСУЕМОСТЬ ЕСТЬ -- в молчащей подложке.** При силе
  связи 10 отвечает 28% сети, а место читается с точностью 0.856; при
  силе 8 отвечает 3% -- и всё равно 0.680. Требование Ф6, закрытое
  отрицательно в v0.12, получает другой ответ. Мешала не линейка и не
  граф, а постоянная спонтанная активность -- черта биологической
  культуры, взятая буквально.
- **v0.22** -- тот же вопрос, что v0.11 фаза 5, но при силе связи 10:
  польза следа опыта **+43.85 п.п.** (23 знака из 24) против +2.33 п.п.
  при силе 1. Дело было в СИЛЕ СВЯЗИ, а не в режиме.

### v0.23-v0.29: работает СОСТАВ, не работает РАСПОЛОЖЕНИЕ

- **v0.23/v0.24** -- перемер двух "немых" уровней при рабочей силе связи.
  Выросшее расположение по-прежнему функционально пусто (вывод v0.14
  подтверждён БЕЗУСЛОВНО). Граница ВПЕРВЫЕ даёт функциональное
  следствие -- и оно ОТРИЦАТЕЛЬНОЕ: адресуемость 0.899 -> 0.809, 11
  сидов из 12. Граница изолирует, изоляция снижает читаемость.
- **v0.25** -- граница НЕ делает области независимыми: обе точности
  падают одинаково. И даже в контроле чтение "своими" не лучше чтения
  "чужими" -- домены есть, но ни одно правило поведения от состояния не
  зависит.
- **v0.26** -- состояние впервые влияет на САМ УЗЕЛ (множитель
  адаптации). Разнородность узлов ПОЛЕЗНА (+8.4 п.п.), её РАСПОЛОЖЕНИЕ
  -- нет (23 из 40 против перемешанных, p = 0.21). Возникает
  разнородный НАБОР, а не области.
- **v0.27** -- вопрос о значении расположения в этой постановке
  НЕРАЗРЕШИМ, и препятствие названо: где граница успевает сложиться,
  нет читаемого отклика; где отклик читаем, граница не успевает.
  Отсюда фазировка развития.
- **v0.28** -- у разметки есть РИСУНОК с масштабом, управляемым отдельно
  от плотности. Попутно выяснилось, что прежняя граница была МЕЛЬЧЕ
  дальности связи (0.250 против 0.285) -- разделение было невозможно по
  построению, и четыре версии мерили несуществующее.
- **v0.29** -- расположение ИМЕЕТ значение ВНУТРИ одной ткани (0.267
  против 0.015), но весь его путь идёт через ТОЛЩИНУ мостика: случайное
  прореживание до той же толщины даёт то же самое (p = 0.9999).

### v0.30-v0.36: опыт -- единственное, что проходит через процесс

- **v0.30** -- добавлено внешнее воздействие. Ткань отгораживается ровно
  от того места, где её касается мир: связи сенсорной поверхности 40.0
  -> 0.6. Функциональная половина забракована собственным контролем
  (ошибка №39: задача решалась геометрией даром, 0.95 даже у ткани,
  никогда не видевшей входа).
- **v0.31** -- задача переделана так, чтобы контроль её не решал. **Вход
  становится читаемее оттого, что ткань росла под ним:** +9.27 п.п.
  против ПОБИТОВО той же сети, выросшей без входа (17 из 20,
  p = 0.0013); бесстроенное воздействие той же силы даёт +1.5 п.п.
- **v0.32** -- разведены две службы `s` ("насколько я был активен" и "с
  кем можно связываться"). Стена снята, знак прибавки остался, величина
  не держится. Побочно: фазировка развития больше не нужна.
- **v0.33** -- встречное правило (активность ПОМОГАЕТ связи) держит
  структуру, но строение входа всё равно не обгоняет простую активность.
- **v0.34/v0.35** -- правило границы НЕ ПОКАЗАНО виновным: при смене
  ровно одной ручки картина не воспроизводится.
- **v0.36** -- шум меры измерен, а не предположен (320 проб: 2.04 п.п.
  против 4.18 при 80). При точной мере прибавка от СТРОЕНИЯ входа
  держится во всех режимах.

### v0.37-v0.45: ткань строит себя сама; процесс пуст, содержание приходит извне

- **v0.37** -- `growth_by_division`: ткань начинается с ОДНОГО элемента и
  строит число элементов и расположение сама. Претензия "организация
  выдаётся готовой" снята структурно. Вердикт о чтении НЕ ВЫНЕСЕН:
  1.84 п.п. при измеренном шуме меры 2.04 (урок №42).
- **v0.39** -- сравнение выравнено по расписанию рождений (ошибки №43,
  №44): выросшее расположение читается на +3.84 п.п. лучше брошенного
  жребием. Первый случай в проекте, когда расположение само по себе
  что-то даёт.
- **v0.40** -- ВЕРДИКТ НЕ ВЫНЕСЕН: контроль "ровной ткани" не
  выравнивается по плотности (14.7% при пороге 10), и код отказался
  читать, как ему было велено.
- **v0.41** -- разложение получено ПЕРЕМЕШИВАНИЕМ порядка рождений, а не
  построением: **работает ГЕОМЕТРИЯ** (ровность укладки +4.48 п.п.,
  p = 0.0086), история роста даёт +1.18 п.п. (p = 0.38, ниже шума меры).
  Рост важен как СПОСОБ получить ровную ткань, а не сам по себе.
- **v0.42** -- рост, следующий за употреблением, помогает (+5.27 п.п.),
  но что помогает именно СООТВЕТСТВИЕ строению мира -- НЕ показано
  (+4.92 п.п. без строения; ошибка №46).
- **v0.43/v0.44** -- след строения мира в ткани ЕСТЬ и крупен, если
  читать ЧИСЛО задетых связей (+18.5%, 27 из 30), а не суммарный сдвиг
  (+1.3%). Правило роста, способное им воспользоваться, НЕ НАЙДЕНО --
  пять переделок механизма, пять названных причин.
- **v0.45** -- рост по совпадению СКЛОНЯЕТСЯ к строению мира по знаку,
  но эффект 0.97 узла из сорока (2.5%) и объявленного порога не берёт.
  Расширять выборку ИМЕННО ПОТОМУ, что вышло близко к порогу, отказано
  сознательно: это выбор момента остановки по результату.

### v0.46-v0.51: петля со средой замкнута -- и ничего не дала

- **v0.46/v0.47** -- петля замкнута: ткань двигает мир (средний u 0.740
  при разомкнутой против 0.679 при замкнутой). Живёт она НЕ ВО ВСЯКОЙ
  ткани: двигательные разряды у 55%, по принципу "всё или ничего".
  Передачу пришлось поднять с 10 до 14, иначе петля мертва -- **это
  смена режима относительно v0.21-v0.45, и числа между версиями надо
  сравнивать с оглядкой на неё.**
- **v0.48** -- замкнутая ткань читается на 2.71 п.п. ХУЖЕ привязанной
  (8 сидов из 21) -- разницы не установлено, намёк в сторону убытка.
  **Предсказание "возможность влиять на мир делает ткань лучше" НЕ
  ПОДТВЕРДИЛОСЬ.** Причина названа устройством: пластичность ничем не
  направлена, ткани нечем отличить удачное движение от неудачного.
- **v0.49-v0.51** -- признак успеха из PRINCIPLES §2b (предсказуемость
  собственного входа) измерен в трёх исполнениях: построчном, посвязном
  и посвязном с сохранением суммы входа. На читаемость: -0.09, -0.97 и
  -0.27 п.п. при объявленном пороге 2. Механизм при этом ДЕЙСТВУЕТ
  (движение весов вдвое сильнее исходного, вырождения нет, ткань не
  глохнет). **Предпосылка §2b НЕ ПОДТВЕРДИЛАСЬ**, обоснование
  "выводится из уже принятой меры" ОТОЗВАНО; предсказание о скуке тоже
  не подтвердилось. Всякий будущий признак успеха придётся обосновывать
  измерением, а не выведением.

### v0.52-v0.58: поворот к ФОРМЕ -- дальние связи

Основание поворота (STAGE_MAP, со слов автора 2026-09-16): у ткани нет
НИ ОДНОЙ дальней связи, она чисто местная решётка, и об это бились все
предыдущие версии. Добавлен `long_range_rate` (тождество при нуле: 9 из
9). Проверка объявлена не как "красивее ли стало", а как "перестала ли
требоваться подпорка" -- то есть оживёт ли петля при обычной силе связи.

- **v0.52** -- тракты оживляют петлю ВШЕСТЕРО и проводят сигнал УЖЕ, чем
  разгон, но до уровня подпорки не дотягивают: 47% живых петель против
  65%.
- **v0.53** -- тракты проводят уже (0.552 отвечавших против 0.586), но
  НЕ ДЕШЕВЛЕ: общая активность выше. Форма меняет РУСЛО, а не РАСХОД.
- **v0.54** -- направленный тракт между ДАЛЁКИМИ областями разметки
  обошёл подпорку по главному признаку (70% живых петель против 60%)
  при пройденном стороже, но провалил третье условие. Вскрылось, что
  объявленный набор условий ВНУТРЕННЕ ПРОТИВОРЕЧИВ: узость вовлечения и
  малая суммарная активность разнонаправлены по построению пути. Прямая
  мера ("доля ткани на один двигательный разряд": 4.68 у тракта против
  8.63 у подпорки) названа ПОСЛЕ взгляда на числа и вердикта не несёт.
- **v0.55** -- тропинка (путь, который ткань выбирает сама по
  СОВПАДЕНИЮ разрядов) выходит самой скромной и самой слабой:
  активность 349 против 484 у подпорки, отклик 0.508, доставка 23.6
  разряда против 67.9. Наименьшее сопротивление даёт СБЕРЕЖЕНИЕ, а не
  дальнобойность: тропа идёт по лугу, а моста в безлюдное место не
  строит. Попутное наблюдение на четырёх случаях: ни один
  самостоятельный механизм в этой модели не начинался с нуля --
  самостоятельность начинается со ВТОРОГО шага, первый приходится
  задать.
- **v0.56** -- **у ткани впервые обнаружилась ИСТОРИЯ.** Одна и та же
  ткань (те же координаты, то же расписание рождений) при разном шуме
  даёт совпадение дальних путей 0.3262 против 0.0012 у случайных
  наборов той же величины (54 из 54, p = 5.6e-17) -- в 270 раз выше
  случайного, но НЕ полное: около половины путей общие, половина своя у
  каждого прогона. Двоичное правило чтения ("диктует геометрия" против
  "выбирает ткань") оказалось слишком грубым для ответа, который есть
  ПРОПОРЦИЯ, и это записано как урок.
- **v0.57** -- **эта история ДЕЙСТВУЕТ.** При побитово равной ближней
  сети, равном числе дальних связей и равных весах перенос дальних
  путей на места чужой истории меняет отпечаток отклика втрое сильнее
  шума пробы (0.04098 против пола 0.01117), у 20 тканей из 20. Второй
  вопрос (ближе ли чужая история, чем случайный перенос) ответа НЕ
  получил: 13 из 20 при p = 0.13, порог был 0.05 -- наклон есть,
  вывода нет. Мера проверена на насыщение отдельной дозовой развёрткой.
- **v0.58** -- **умение зависит от истории:** разброс умения по историям
  одной ткани 0.0496 против шумового 0.0197, у 64 тканей из 81,
  размах 11.03 п.п. **Но даёт его ЧИСЛО путей, а не их МЕСТО:** при
  разном числе дальних связей умение расходится на 8.56 п.п., при
  РАВНОМ -- на 2.77 при шуме 2.42, и знак неустойчив (27 из 51,
  p = 0.39). Прежний урок проекта ("важно какие связи, а не сколько")
  этим СУЖАЕТСЯ: он про различение, а не про пропускную способность --
  два разных свойства, мерить их надо порознь. Ошибка №49 (правило
  чтения без пустого отсчёта) записана здесь же.

---

## Текущее состояние (на 2026-09-20, последняя завершённая версия -- v0.58)

**Критерий зачатка закрыт по всем пяти пунктам** (STAGE_MAP): ткань
строит пространственную организацию сама, разводит области с
устойчивым масштабом, принимает и различает воздействие извне,
перестраивается под него так, что оно читается лучше, растит число
элементов и меняет форму. Ничего из этого ей не выдано готовым.

**Сквозная линия результатов, на трёх уровнях сразу.** Работает СОСТАВ
(какие элементы, как разложены, какие веса) и работает ОПЫТ. Не
работает ПРОЦЕСС сам по себе: значение места связей сводится к толщине
мостика (v0.29), значение роста -- к ровности укладки (v0.41), значение
следа строения мира живёт в ЧИСЛЕ задетых связей, а не в величине
сдвига (v0.44). Исключение одно: процесс содержателен ровно тогда,
когда через него проходит СОДЕРЖАНИЕ ИЗВНЕ (v0.31, v0.36).

**Что появилось у ткани за последние три версии и чего не было прежде
-- собственное прошлое.** До v0.56 всё построенное выводилось из
начальных условий; теперь половина дальних путей складывается по ходу
дела (v0.56), эта половина входит в то, что ткань делает (v0.57), и у
разных прошлых одной ткани разное умение (v0.58) -- но различает их
пропускная способность, а не рисунок связей.

**Что измерено и НЕ подтвердилось -- этого нельзя терять при пересказе:**
- замкнутая петля со средой работает механически и выигрыша не даёт
  (v0.48);
- признак успеха §2b не дал ни пользы, ни предсказанной цены в трёх
  исполнениях подряд, и его обоснование отозвано (v0.49-v0.51);
- порядок предъявления сеть по-прежнему различить не может -- линия
  v0.11 фазы 2 закрыта конструктивным, а не параметрическим
  ограничением;
- форма (тракты) пока меняет русло, а не расход (v0.53), и по
  объявленному заранее правилу подпорку не заменила (v0.54).

**Незакрытое, и это самое крупное.** Пункты 9 и 11 дорожной карты --
интеграционные структуры и обучение взаимодействию со средой. Петля
замкнута, но ткани нечем отличить удачу от неудачи, а единственный
предложенный признак успеха измерен и провалился. Пока признака нет, у
"обучения взаимодействию" нет предмета.

**Открытое направление, вытекающее прямо из v0.58** (приоритет НЕ
назначен, решение за автором): первое, что здесь вообще можно улучшать,
-- не рисунок связей, а то, насколько охотно ткань их прокладывает.
Место и число путей ведут себя как два РАЗНЫХ свойства (место меняет,
ЧТО ткань отвечает; число -- сколько из ответа читается), и дальше их
надо вести порознь.

---

## Ограничения воспроизводимости v0.12-v0.58

Продолжение раздела "Ограничения воспроизводимости" выше; там -- про
v0.1-v0.11.

- **Смена режима в середине линии.** До v0.21 всё меряли при силе связи
  1 в занятой сети; v0.21-v0.45 -- при силе 4-10 в молчащей подложке;
  v0.46+ -- при 14, иначе петля мертва. Числа РАЗНЫХ версий сравнимы
  только внутри своего режима, и старые числа сознательно не
  переписаны: они верны для своего режима и помечены.
- **Отбраковка тканей в v0.56-v0.58 крупная и односторонняя.** v0.57:
  65 тканей из 96 отброшены за малым числом собственных дальних
  связей, у оставшихся их 58.3 -- вдвое выше среднего по v0.56; вывод
  относится к ткани, ПРОЛОЖИВШЕЙ МНОГО ПУТЕЙ. v0.58: 88 из 200
  отброшены за отсутствием отклика у дальних узлов; вывод относится к
  ткани, у которой дальняя передача вообще состоялась.
- **v0.20 отозван целиком** (ошибка №34) -- его числа не используются
  нигде; вопрос закрыт заново в v0.21.
- **v0.37, v0.40 -- вердикт не вынесен по объявленному заранее правилу**
  (величина ниже измеренного шума меры; контроль не выравнялся по
  плотности). Это не отрицательные результаты и не положительные -- их
  нельзя цитировать ни в ту, ни в другую сторону.
- **v0.54** -- объявленный набор условий оказался внутренне
  противоречив; прямая мера названа после взгляда на числа и вердикта
  НЕ НЕСЁТ. Какая из двух мерок отвечает замыслу -- вопрос к автору, а
  не к измерению.
- **v0.57, второй вопрос** -- НЕ ОТЛИЧИМО (p = 0.13 при объявленном
  пороге 0.05). Менять меру на более чувствительную здесь запрещено
  тем же правилом, что ловило ошибки №36, №46 и разбор v0.56.
- Число сидов в v0.56-v0.58 назначалось ДО чтения направления (v0.57:
  расширение с 24 до 96 тканей решено по ЧИСЛУ, а не по исходу, и это
  записано в заголовке прогона).

---

## Вторая линия проекта: движок (`engine/`)

В репозитории живут ДВА проекта, и их не надо путать.

| | Mozg (`code/`, `docs/`) | digital-life-engine (`engine/`) |
|---|---|---|
| язык | Python + numpy | Node.js, без зависимостей |
| журнал | `docs/RESULTS_LEDGER.md` (v0.1-v0.58) | `engine/DEVELOPMENT.md` (этапы 1-13) |
| предмет | импульсная ткань: связи, пластичность, разметка, дальние пути | агенты, поля, геном как правило, механика ткани и формы |
| текущее | v0.58 | этап 13: упругий слой даёт изгиб (прогиб/остаток 2.27 против 0.68) |

`docs/MERGE_ASSESSMENT.md` (2026-09-14) разбирал, можно ли их соединить:
прямое слияние кода не рекомендовано, линии дополняют друг друга по
дорожной карте. **Рекомендация того документа -- "основной линией сделать
движок" -- фактически НЕ ИСПОЛНЕНА:** после 14.09 вся работа шла в
Python-линии (v0.42 -> v0.58). Расхождение между рекомендацией и
практикой не разрешено и ждёт решения автора; до тех пор MERGE_ASSESSMENT
читать как разбор, а не как принятый план.

Журнал движка ведётся ТОЛЬКО в `engine/DEVELOPMENT.md`. Его устаревшая
копия в корне репозитория (этапы 1-10, без 11-13) удалена 2026-09-20 как
параллельный источник истины -- ровно то, что запрещает RUNBOOK.

---

## Среда и запуск (проверено 2026-09-20)

- **Движок** запускается как есть: `cd engine && node test/parity.js` --
  все проверки проходят (Node v22.22.2).
- **`digital-life-engine.tar.gz` в корне -- НЕ мусор и не устаревшая
  копия движка, а ЭТАЛОН.** Это снимок ядра ДО закреплённых контактов
  (этап 10), и `engine/test/junction_identity.js` сверяется именно с
  ним: без него проверка тождества при `junction = 0` (та самая, на
  которой стоит весь этап 11) перестаёт запускаться из коробки.
  Проверено 2026-09-20: 6 сверок из 6. Команда запуска -- в
  `engine/README.md`, раздел "Эталон для проверки тождества".
- **Python-линия** в свежей среде НЕ запускается без подготовки:
  numpy не установлен (`pip install numpy matplotlib
  --break-system-packages`), а `data/` отсутствует -- директория
  версионированием не покрыта (см. `.gitignore`) и восстанавливается
  распаковкой `v0.1-v0.10_complete.tar.gz` с проверкой по
  `v0.1-v0.10_complete_MANIFEST.sha256` (`tools/verify_manifest.py`).
  Порядок работы -- RUNBOOK.md, включая список модулей, которые
  запускают тяжёлые вычисления ПРИ ИМПОРТЕ.
- При переносе в новую среду повторять три проверки побитовой
  воспроизводимости из RUNBOOK ("Перенос в новую среду"), а не
  полагаться на запись о прошлом переносе.

## Ближайший согласованный шаг

**НЕ ВЫБРАН** (запись обновлена 2026-09-20 после v0.58; прежняя
формулировка относилась к концу пакета v0.1-v0.10 и заменена).

v0.58 завершена и зафиксирована. Приоритет следующего шага сознательно
НЕ назначен -- решение за автором. Направления, вытекающие из самих
измерений, а не из желания продолжать по накатанной:

1. **Пропускная способность вместо рисунка.** v0.58: всё, что прошлое
   даёт умению, сводится к ЧИСЛУ проложенных дальних путей. Значит
   первое, что здесь можно улучшать, -- охотность прокладывания, а не
   место связей.
2. **Место путей отдельно от их числа.** v0.57 и v0.58 расходятся не
   случайно: место меняет, ЧТО ткань отвечает, но не то, сколько из
   ответа читается. Это два разных свойства, и мерить их надо порознь.
3. **Признак успеха.** После v0.49-v0.51 §2b отозван в части
   обоснования. Без признака успеха пункт 11 карты (обучение
   взаимодействию со средой) не имеет предмета -- но всякий новый
   признак придётся обосновывать измерением, а не выведением из §2a.
4. **Противоречие мерок формы (v0.54).** Что считать "хорошим" путём --
   узость вовлечения или малую суммарную активность -- вопрос к автору,
   а не к измерению; пока он не решён, тракты нельзя ни засчитать, ни
   отвергнуть.
5. **Расхождение между MERGE_ASSESSMENT и практикой** -- какая из двух
   линий (Python-ткань или движок) основная. Не решено с 14.09.

Правило прежнее: следующая сессия начинает с ВЫБОРА направления, а не с
предположения, что продолжение -- это обязательно v0.59 по накатанной
колее.

## Открытые вопросы первой эпохи (гипотезы, НЕ факты)

*Список составлен на конец v0.10 и сохранён как записан. Один его пункт
получил ответ позже: "как определить полезность проверяемым образом" --
построена задача с ценой ошибки (v0.11 фаза 5), а затем измерена при
рабочей силе связи (v0.22). Остальные остаются открытыми либо потеряли
предмет вместе со сменой режима и правил роста после v0.13.*

- Является ли доминирование штрафа длины над бюджетом (v0.5) специфичным
  для выбранных констант (`K=12`, `scale=0.10`) или устойчивым свойством
  правила в широком диапазоне параметров? -- не проверено.
- Связан ли межобластной "провал" эффекта в v0.4 с конкретным выбором
  4 регионов по сетке 2x2, или с той же картиной столкнёмся при другой
  геометрии/числе регионов? -- не проверено.
- Существует ли осмысленная "функциональная специализация" регионов,
  которая переживёт независимую проверку на нескольких структурах? --
  открытый вопрос, для этого и задумана v0.5.
- Связь периода ритма с константой адаптации (v0.3) выглядит примерно
  линейной на 3 точках -- это не доказанный линейный закон.
- Снижение `F_200` при штрафе длины на t=12с (v0.5) в основном
  ОБЪЯСНЕНО задержкой развития для условий БЕЗ бюджета (v0.6: контраст
  исчезает к t=96с вместе со сближением графов). ДЛЯ УСЛОВИЙ С БЮДЖЕТОМ
  вопрос не закрыт по-другому: структурное различие устойчиво (Жаккар
  ~0.77), но функциональный контраст не сохраняет направление (близок к
  нулю на 2 геометриях, разворачивается на третьей) -- то есть не
  "объяснён", а скорее "не воспроизводится тем же способом". Причина
  этой неоднородности (веса? пороги? конкретный состав рёбер?) не
  установлена.
- Обобщается ли наблюдение v0.6 "устойчивое структурное различие не
  определяет направление функционального различия" на другие метрики
  (не только `F_200`), другие протоколы стимуляции или другие
  диапазоны бюджета/масштаба штрафа? -- не проверено, наблюдение сделано
  на одной конкретной метрике и одном протоколе.
- Как определить "полезность" изменения связей проверяемым образом,
  прежде чем специфицировать эксперимент с перестройкой контактов
  (следующий этап)? -- открытый методологический вопрос, не факт о
  модели.
- v0.7: отсутствие согласованного функционального эффекта перестройки
  за 24с/48 событий -- следствие того, что эффект действительно мал/
  отсутствует, или горизонта/числа событий недостаточно, чтобы отделить
  его от разброса по стимулам и структурам? Не проверено (не пытались
  продлить или увеличить число событий -- сознательно, чтобы не задним
  числом добиваться желаемого результата). Также: близкая частота и
  нулевая доля молчащих узлов не устанавливают сохранение ВСЕЙ временной
  организации активности -- только этих двух конкретных показателей.
- v0.8 (сформулирован, не специфицирован): может ли совместная работа
  пластичности весов и оборота контактов создавать структурный след
  повторяющегося входа, отличимый от следа случайного оборота той же
  интенсивности? Ключевая методологическая развилка не решена: как
  контролировать число замен между условиями и как отличить эффект
  конкретного входа от обычной спонтанной пластичности.
