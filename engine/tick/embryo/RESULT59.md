# Результат шага 59: строка 8 -- НЕТ, мера проверена; порядок есть во входах, ткань его не берёт

Мера -- `PRE58_LINES_5_8.md`, закоммичена до кода. Проба `ORDER` (каналы A, B, Q) и её
нуль `ORDERSHUF`. При `ORDER=0` движок побитово прежний (`id59.js`; движок до шага --
`history/neuron2_run4b.js`). Проба проверена на 50000 кругах: у всех трёх каналов
среднее 0 и дисперсия 1.00, порядок A→B -- в 49.2% эпизодов.

Зародыш с пробой, сиды 3201-3212, 100000 кругов, окно 80000-100000 (~1480 отчётов на
сид). Замер -- `order.js`, строки -- `out/order59.tsv`.

| условие | доля верного знака у ткани | линейный предсказатель по входам | частей в канале Q |
|---|---|---|---|
| порядок | **0.501** (по сидам 0.49-0.52) | **1.000** | 4 из 8 |
| нуль (знак -- жребий) | 0.503 | 0.538 | 4 из 8 |

## Проверки меры -- выполнены

- **Положительный отсчёт:** линейный предсказатель по зашумлённым датчикам A и B с
  запаздываниями 0-6 угадывает порядок **в 100% отчётов** (порог 0.85). Задача
  решаема из того, что ткань видит, и даже линейно.
- **Нуль:** 0.503 (допуск 0.45-0.55).
- **Страж:** миры живы (101 часть).

## Строка 8 -- НЕТ

Доля верного знака **0.501** при пороге 0.75 -- ровно случайный уровень, во всех
двенадцати сидах.

## Что это значит

Нехватка другая, чем у строки 5. Информация о порядке **целиком лежит во входах
ткани**, линейно, на глубине до 6 кругов. Ткань её не берёт. Замечено, не проверено:

- канал Q стоит на 4 частях из 8 -- тот же «проходной двор» бедного канала, что у
  медленного канала до шага 41: кто не умеет предсказать, тот не зарабатывает и не
  доживает до умения;
- чтобы сравнить два события с разрывом до 5 кругов, нужна память входов глубиной
  до 6 кругов. У зародыша передатчики глубины 2 (8 → 0 → 9), вес на себя -- один след
  на часть.

## Предсказание

Проверки выполнены, строка 8 -- НЕТ, доля около 0.5-0.6 -- **да** (0.501).
