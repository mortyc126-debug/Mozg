# Результат шага 63: новая статистика строки 0 -- отношение сумм по сидам; строка 0 держится

Предрегистрация -- `PRE63_HSTAT.md`, закоммичена до кода. Действующий зародыш, сиды
3701-3712, 100 циклов на длину тишины, суммы по половинам. Замер -- `hstat.js`, чтение --
`readhstat63.js`, строки -- `out/hstat63.tsv`.

| | прежняя `H` (медиана по сидам) | **А: отношение сумм по сидам** | Б: аналитический эталон |
|---|---|---|---|
| `T = 10` | 0.859 | **0.880** ± 0.018; половины 0.874 / 0.886 | 0.978, повторяемость 0.132 |
| `T = 30` | 0.625 (по сидам от −0.11 до 1.01) | **0.637** ± 0.056; половины 0.600 / 0.678 | 0.597, повторяемость 0.475 |

## Проверки

- **А:** разброс по бутстрепу сидов при `T = 30` -- **0.056** (порог 0.10); половины
  расходятся на **0.078** (порог 0.15). При `T = 10` совпадает с прежней мерой (0.880 против
  0.859, допуск 0.1). **Проходит.**
- **Б:** повторяемость при `T = 30` -- 0.475 (порог 0.15), при `T = 10` расходится с прежней
  (0.978 против 0.859). **Не проходит.** Шум у неё и в числителе: доля объяснённой
  дисперсии по одному сиду на 100 значениях `L` сама шумит, а эталон-постоянная, в отличие
  от выборочного оптимума, этот шум не гасит.

**Выбор -- статистика А.** Она заменяет прежнюю меру строки 0 в батарее с шага 63
(`BATTERY.md`, поправка шага 63; `battery.js` и `readbattery.js` переведены на неё; прогоны
1-5 остаются по прежней мере).

## Строка 0 у действующего зародыша -- ЕСТЬ

`H(10)` **0.880** ± 0.018, `H(30)` **0.637** ± 0.056 при пороге 0.5. Порог при `T = 30` --
на двух стандартных отклонениях ниже значения. Оговорка шага 62 снята: статус строки 0
теперь не внутри шума меры.

## Предсказания §5

1. А проходит, бутстреп около 0.05 -- **да** (0.056).
2. Б проходит, около 0.1 -- **нет** (0.475).
3. При `T = 10` обе совпадают с прежней в пределах 0.05 -- **наполовину**: А -- 0.021, Б -- 0.119.
4. Строка 0 -- ЕСТЬ, `H(30)` около 0.5-0.6 -- **да** (0.637).
